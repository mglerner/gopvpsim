title = "How This Works"
slug = "how-this-works"
description = "How these dives get built: PvPoke's simulator, reimplemented in Python, swept across every IV, then rendered with Plotly. The short version of what you can and can't trust."
order = 0
coming_soon = true

# When this guide's body.md lands, set coming_soon=false.
# Tokens come from docs/verification_counts.toml (dev:*) and the
# Oinkologne Male GL reference dive (dive:*).
