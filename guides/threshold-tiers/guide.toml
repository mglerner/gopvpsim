title = "Threshold Tiers"
slug = "threshold-tiers"
description = "Tier vs category vs anchor. What the stat-cutoff bullets mean and why overlapping tiers are intentional."
order = 1
coming_soon = false
authorship = "both"

# Reference dive is inherited from build_guides.DEFAULT_REFERENCE
# (Corviknight GL as of 2026-09-22). Numeric citations resolve via
# dive:* tokens so the worked example auto-updates on every publish.
# The worked example no longer assumes which axes the top tier cuts
# on: dive:top_tier_axes / dive:top_tier_rule print whatever it
# carries, so a reference repoint needs no prose edit here.
