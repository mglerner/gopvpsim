# ARCHIVED SPREADS -- retired 2026-09-09, NOT loaded by anything.
#
# These are the human-sourced spreads for altaria, lifted verbatim out of
# thresholds/altaria.toml when the Twilight Trails move rebalance invalidated
# them. Values, descriptions and sources are unmodified.
#
# WHY THEY WERE RETIRED. A spread cutoff is the stat at which a specific
# matchup flips, so it is only true against a specific opponent kit. The
# rebalance changed a move in the DEFAULT moveset of 32 of the 86 opponents
# named across the threshold files, so most cutoffs no longer land on a real
# breakpoint. Leaving them live would have kept a named community member's
# `source` attribution attached to numbers the rebalance had made wrong.
#
# WHAT IS ACTUALLY VALUABLE HERE. Not the numbers -- the `description`
# fields. "Premium bulk: Azu bulkpoint + Medicham survival + all lower def
# checks" is a SPECIFICATION that re-derives mechanically once a bake
# recomputes those anchors, and it encodes the human judgment about which
# thresholds matter, which is the part that cannot be regenerated. Read this
# file as a work queue, not as data.
#
# THE ENGINE THESE WERE TRUE UNDER -- to reconstruct it:
#
#   pvpoke commit   79d04af74   (last vetted pre-rebalance; the pin in
#                                tests/fixtures/pvpoke_engine_digests.json
#                                at the time of retirement)
#   turn system     LEGACY      (charged moves did NOT take priority over
#                                fast damage; the live game stopped running
#                                this on 2026-09-02)
#   gamemaster      cd ~/coding/pvpoke &&
#                   git show 79d04af74:src/data/gamemaster.json
#   our engine      git log --oneline -1 fba4d4c   (repo HEAD when the
#                                rebalance landed; engine-hash files are
#                                battle.py, _dp_jit.py, moves.py,
#                                formchange.py, pokemon.py)
#
# Note the turn system: these came from community in-game testing under the
# LEGACY clock. Re-deriving them needs BOTH the old gamemaster and
# --mechanics legacy, and a number reproduced under only one of the two is
# not a reproduction.

[Altaria.Great.spreads."Trev Slayer"]
attack = 102.97
defense = 149.02
stamina = 139
description = "RyanSwag's 'Trevenant Slayer' tier: 102.97 atk, 149.02 def + 139 hp. Atk BP for Trevenant 1-2/2-2 plus def bulkpoint for Trevenant 1-1/0-1, both calibrated vs max-def Trevenant, while keeping the 2-2 mirror-tie HP. His general-safety pick; sort by def."
source = "RyanSwag GamePress Altaria deep dive (2022-10-27); mirrored in gobattlekit SENTIMENTAL_DEFAULT_THRESHOLDS 'Trev'"

[Altaria.Great.spreads."Bulk + Trev"]
attack = 101.95
defense = 151.63
stamina = 139
description = "RyanSwag's 'Bulk + Trevenant maybe' tier: 101.95 atk, 151.63 def + 139 hp. For when you don't want to forgo the bulk life and don't expect high-def Trevenant; the def cutoff also covers the Water Gun Lanturn checks. Tie-breakers: + 140 hp generally better, and 153.95 def more useful than + 141 hp."
source = "RyanSwag GamePress Altaria deep dive (2022-10-27); mirrored in gobattlekit SENTIMENTAL_DEFAULT_THRESHOLDS 'Bulk+Trev'"

[Altaria.Great.spreads."Bulk Focus"]
attack = 0
defense = 153.95
stamina = 139
description = "RyanSwag's pure-bulk tier: 153.95 def + 139 hp. 139 hp is the golden ticket that ties the 2-2 mirror (and Walrein 2-1); the 139-141 hp checks (Vigoroth 2-2, Venusaur 1-2, Umbreon 1-1) all likely require this def. His verdict: aside from ranks 1, 2 & 7, probably just go for the atk-weighted builds."
source = "RyanSwag GamePress Altaria deep dive (2022-10-27); mirrored in gobattlekit SENTIMENTAL_DEFAULT_THRESHOLDS 'Bulk'"

# ─────────────────────────────────────────────────────────────────────
# Anchors — damage breakpoints (atk-side), current-meta close matchups
# (opponents from gl_top50_plus_cs.txt 2026-06-10)
# ─────────────────────────────────────────────────────────────────────

# Jumpluff: in the current pool AND a RyanSwag calibration point. Grass/
# Flying with Fairy Wind pressure; the fast-move chip war is tight both
# ways, so single damage steps decide the 1-1 and 0-1.
