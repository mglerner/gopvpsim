# Aegislash (Blade) -- stub threshold file.
#
# Aegislash (Blade) isn't in PvPoke's GL/UL rankings. The species exists
# in the gamemaster with atk 272 / def 97 / hp 155 as an "always-Blade"
# hypothetical form; PvPoke's canonical simulated Aegislash starts in
# Shield and transforms mid-battle. This stub exists so downstream
# article/comparison tooling can find an article slug for the form.
#
# Deep dive: userdata/website/aegislash-blade-great-league/ (GL).
# Articles:  articles/aegislash-form-change-guide-gl.toml (GL).
#
# UL coverage was dropped 2026-05-17 per an HSH Discord member's review: UL
# Aegislash is not competitively viable.
#
# deep_dive.py is invoked with `--no-thresholds` for Aegislash so this
# file isn't auto-loaded into the threshold registry; no attack/defense/
# stamina spreads are authored here. Upgrade to a full spread file if
# enough anchor work accumulates to warrant it.

["Aegislash (Blade)"]
sources = "pogo-simulator hypothetical always-Blade dive (no PvPoke ranking); form-change mechanic is modeled natively by src/gopvpsim/formchange.py but the realistic path starts from Shield."

# Species narrative (Shape 2 Session 3, 2026-04-19). Aegislash (Blade)
# is a diagnostic hypothetical, not a realistic build target. The
# auto-gen templates in scripts/auto_gen_narrative.py only fire for
# species with a cd_prep block, so these stay empty until a human
# writes Aegislash-specific prose. Section renders only when non-empty.
["Aegislash (Blade)".intro]
body = ""

["Aegislash (Blade)".meta_role]
good_at = ""
bad_at = ""
body = ""
