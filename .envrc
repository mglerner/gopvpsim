# direnv: auto-activate the uv-managed project venv when you enter this dir
# (shell + Emacs/eglot). `uv venv` created .venv; run `direnv allow` once to
# trust this file. Re-run `uv venv` on a new machine, then `direnv allow` again.
export VIRTUAL_ENV="$PWD/.venv"
PATH_add "$VIRTUAL_ENV/bin"
